/* ----------- ORDER APIs test ---------------
    In this test file all ORDER APIs are tested including: 

    ---------- USER APIs -------------------
    1. POST /order - Create Or Update ORDER
    2. GET /user-orders- List All USER ORDERS
    3. GET /user-orders/:_id - Read USER ORDER

    ---------- ADMIN APIs -------------------
    1. GET /orders - List All ORDERS
    2. DELETE /order/:_id - Delete ORDER
    5. POST /order-delete-many - Delete Many ORDERS
    2. POST /update-order-status - UPDATES ORDER STATUS
  
  
  
*/

describe("Order API", () => {
  it("Order Test", async () => {
    return true;
  });
});
