/* ----------- CATEGORY APIs test ---------------
    In this test file all category APIs are tested including: 

    ---------- PUBLIC APIS --------------
    1. GET /category/:slug - Read Brand
    2. GET /categories/list-all - List all categories

    ---------- ADMIN APIs -------------------
    1. GET /categories - List All Brands
    2. POST /category - Create Or Update Category
    3. DELETE /category/:slug - Delete Category
    4. POST /bulk-category - Create Or Update Category
    5. POST /category-delete-many - Delete Many Categories
  
*/

describe("Category API", () => {
  it("Category Test", async () => {
    return true;
  });
});
