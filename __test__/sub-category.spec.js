/* ----------- SUB-CATEGORY APIs test ---------------
    In this test file all SUB-CATEGORY APIs are tested including: 

    ---------- PUBLIC APIS -------------------
    1. GET /sub-category/:slug - Read Brand
    2. GET /sub-categories/list-all - List all sub-categories
    3. GET /sub-categories - List all sub-categories

    ---------- ADMIN APIs -------------------
    1. POST /sub-category - Create Or Update Brand
    2. DELETE /sub-category/:slug - Delete Brand
    3. POST /sub-category-delete-many - Delete Many Brands
    
  
*/
describe("Sub-Category API", () => {
  it("Sub-Category Test", async () => {
    return true;
  });
});
