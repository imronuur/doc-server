/* ----------- CLIENT APIs test ---------------
    In this test file all client APIs are tested including: 

    ---------- ADMIN APIs -------------------
    1. GET /clients - List All Clients
    2. POST /client - Create Or Update Client
    3. DELETE /client/:_id - Delete Client
    4. POST /client-delete-many - Delete Many Clients
    5. GET /client/:_id - READ Client
  
*/
describe("Client API", () => {
  it("Client Test", async () => {
    return true;
  });
});
